sky:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: localhost
    port: 3306
    database: sky_take_out
    username: root
    password: 123456
  # ✅ 正确写法（从环境变量读取，无明文）
  sky:
    oss:
      access-key-id: ${OSS_ACCESS_KEY_ID}    # 环境变量占位符
      access-key-secret: ${OSS_ACCESS_KEY_SECRET} # 环境变量占位符
      bucket-name: ${OSS_BUCKET_NAME}        # 环境变量占位符
      endpoint: ${OSS_ENDPOINT}              # 环境变量占位符
